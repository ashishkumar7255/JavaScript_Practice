var btn = document.querySelector("button");
var newtext = document.querySelector("p");
btn.addEventListener("click", function () {
    newtext.textContent = "Select the first button on the page Select the first paragraph on the page Add a click event listener to the button When the button is clicked, clear the text inside the paragraph"
})